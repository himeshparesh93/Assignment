import Resource from '@root/res';
import {colors} from '@root/theme';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {position: 'absolute', top: 40, right: 15, zIndex: 1},
  image: {
    width: 30,
    height: 30,
  },
});

export default styles;
