import {colors} from '@root/theme';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  heart: {
    position: 'absolute',
    top: '38%',
    left: '25%',
    fontSize: 150,
    color: colors.colorBE1E2D,
    zIndex: 2,
  },
});

export default styles;
