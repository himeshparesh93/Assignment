import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignSelf: 'center',
    position: 'absolute',
    marginTop: 80,
  },
  image: {
    height: 34,
    width: 34,
  },
});

export default styles;
